apiVersion: batch/v1
kind: Job
metadata:
  name: schema-loader
spec:
  template:
    spec:
      containers:
      - name: schema-loader
        image: rkube01/schema-loader:v1
        command: ["perl",  "-Mbignum=bpi", "-wle", "print bpi(2000)"]
      restartPolicy: Never
  backoffLimit: 4